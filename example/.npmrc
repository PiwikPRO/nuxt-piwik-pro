# Pack `file:..` as a real directory instead of a symlink — otherwise the library
# resolves `nuxt/kit`/`nuxt/app` from the repo root node_modules (Nuxt leak) and
# loses its `exports` map (the /composables subpath fails to resolve).
install-links=true
